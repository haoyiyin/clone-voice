# Qwen3-TTS Voice Clone dependencies
# Install: pip install -r requirements.txt

qwen-tts>=0.1.0
soundfile>=0.12.0
transformers>=4.40.0
accelerate>=0.28.0
torch>=2.2.0

# Optional (CUDA only, for faster inference):
# flash-attn>=2.5.0
